-- Справочник CEFR-уровней (A1–C2) с описаниями и весами сложности

CREATE TABLE IF NOT EXISTS study_levels (
  level_id INTEGER PRIMARY KEY,                     -- Уникальный ID уровня (например, 1=A1, 6=C2)
  level_client TEXT,                                -- Название уровня для пользователя (например, "начальный", "продвинутый")
  level_word TEXT,                                  -- Текстовое название уровня (например, "Начальный") для соответствия с study_level
  level_category_code TEXT,                         -- Дополнительный код категории (например, "начальный_a1")
  level_group_code TEXT,                            -- Группа сложности (например, "начальный", "средний", "продвинутый")
  description TEXT,                                 -- Подробное описание уровня
  weight_value NUMERIC(3,2)                        -- Вес уровня для расчёта quiz_weight (например, 1.00, 1.30, 1.60)
);


--  UNIQUE(level_group_code)                          -- Гарантирует уникальность группировки

-- 📌 Эта таблица:
-- справочник CEFR-уровней, к которым привязывается логика викторин
-- используется в расчётах quiz_weight, рейтингов, прогресса
-- участвует в связке через VIEW view_study_level_mapped и др.

